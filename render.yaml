databases:
  - name: Gone_Coins
    databaseName: gone_coins
    user: gone_coins_user

services:
  - type: web
    name: gone-coins
    env: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: Gone_Coins
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false